{{- if .Values.ethstats.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "linea.ethstats.serviceAccountName" . }}
  labels:
    {{- include "linea.labels" (dict "context" . "component" .Values.ethstats.name "name" .Values.ethstats.name) | nindent 4 }}
  {{- with .Values.ethstats.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
automountServiceAccountToken: {{ .Values.ethstats.serviceAccount.automount }}
{{- end }}
